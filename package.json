{
  "name": "warframe-mr-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create warframe-mr-tracker",
    "db:init": "wrangler d1 execute warframe-mr-tracker --file=schema.sql",
    "db:init:remote": "wrangler d1 execute warframe-mr-tracker --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
